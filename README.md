# EggLedgerSyncServer

Cloud sync server for [EggLedger](https://github.com/DavidArthurCole/EggLedger).

Provides encrypted blob storage for EggLedger app data (accounts, settings, and reports) via Discord OAuth2 authentication.
The server stores only ciphertext, encryption keys never leave the client.
